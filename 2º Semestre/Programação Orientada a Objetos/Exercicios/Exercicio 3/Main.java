// Exercício 3:Introdução à POO
// Gerenciador de Produto (Estoque Simples)
// Objetivo: Praticar a criação de uma classe, definição de atributos privados, uso de métodos construtores, implementação de métodos de acesso (Getters/Setters) e criação de métodos de comportamento que validam o estado do objeto (Encapsulamento).
// Enunciado:
// Crie uma classe chamada Produto que representará um item de uma loja.
// Esta classe deve possuir os seguintes atributos privados: nome (String), preco (double) e quantidadeEmEstoque (int).
// Crie um método construtor que receba o nome e o preco do produto. A quantidadeEmEstoque deve sempre iniciar em 0.
// Crie os métodos Getters e Setters para nome e preco. Crie apenas o Getter para quantidadeEmEstoque (o estoque não deve ser alterado diretamente via Setter).
// Crie dois métodos de comportamento:
// adicionarEstoque(int quantidade): Soma a quantidade informada ao estoque atual (apenas se a quantidade for maior que zero).
// vender(int quantidade): Subtrai a quantidade informada do estoque. O método deve verificar se a quantidade solicitada é válida e se há estoque suficiente. Caso não haja, exiba a mensagem: "Estoque insuficiente para a venda."
// exibirResumo(): Imprime no console o nome do produto, seu preço e a quantidade atual em estoque.
// Crie uma classe principal (Main) para testar sua classe Produto. Instancie um produto, adicione estoque, tente vender uma quantidade válida, tente vender uma quantidade maior que o estoque e exiba o resumo ao final.
// Teste as possibilidades positivas, negativas e casos extremos.

public class Main {
    public static void main(String[] args){
        

    }
}
